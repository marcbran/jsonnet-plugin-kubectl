# jsonnet-plugin-kubectl

[jpoet](https://github.com/marcbran/jpoet) plugin for read-only Kubernetes queries via [client-go](https://github.com/kubernetes/client-go), similar to `kubectl get`.

See `main.libsonnet` and `examples.libsonnet`.
